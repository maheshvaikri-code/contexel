"""Benchmark suites for contexel.

Run with:  python -m benchmarks        (writes benchmarks/RESULTS.md)

The suites measure the claims contexel actually makes — determinism, budget
enforcement, zero-inference-cost speed, tokenizer accuracy — and characterize
token reduction as a function of the data, deliberately not as one headline
number (see README: "How much does it cut?").
"""
