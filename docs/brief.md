# Brief — contexel release surfaces (PyPI prep + docs site)

- **Requested by:** Mahesh Vaikri, 2026-07-26 ("lets prepare for PyPI … PyPI
  README and Repo README … proper narratives … architecture.html more
  interactive, brighter background … gitpages and wiki style pages")
- **Scope:** PyPI packaging readiness (dual READMEs, metadata, buildable
  artifacts); redesigned interactive architecture page; a wiki-style static
  docs site suitable for GitHub Pages. No library behavior changes.
- **Out of scope:** the PyPI upload itself, enabling Pages in repo settings,
  and pushing to GitHub — each is a §5 external action for the human.
- **Web publish:** pages (opt-in recorded here per `skills/web-publishing.md`;
  designated source = `site/` only; doctrine artifacts in `docs/` are never
  published). Wiki = wiki-*style* pages in the site, not the GitHub wiki
  product (repo has no remote wiki yet); one-way docs→wiki sync can be added
  later if wanted.
- **Cloud target:** n/a (GitHub Pages only).
- **Company profile:** startup=off enterprise=off.
