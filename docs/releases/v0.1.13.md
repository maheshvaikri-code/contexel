# Release record — contexel v0.1.13 (first PyPI release)

- **Published:** 2026-07-26, via `.github/workflows/release.yml` on tag
  `v0.1.13` (human-created tag = the release decision).
- **Artifacts:** `contexel-0.1.13-py3-none-any.whl`,
  `contexel-0.1.13.tar.gz` — both `twine check` PASSED pre-publish.
- **Live metadata verified** via `https://pypi.org/pypi/contexel/json`:
  version 0.1.13, `requires_python >=3.9`, readme `text/markdown`, all five
  project URLs present; the raw-GitHub logo URL returns HTTP 200 so the
  PyPI page renders complete.
- **Post-release verification (release-manager mandate), real output:**

  ```
  $ verify-venv/Scripts/python -m pip install contexel==0.1.13   # clean venv
  installed from PyPI: 0.1.13
  select              12 -> 12      1,141 -> 1,105   tokens
  dedupe              12 -> 4       1,105 -> 368     tokens
  rescore              4 -> 4         368 -> 387     tokens
  rank                 4 -> 4         387 -> 387     tokens
  trim_to_budget       4 -> 4         387 -> 387     tokens
  --------------------------------------------------------
  total                             1,141 -> 387     tokens  (66% reduction)
  quickstart OK: 4 records in context
  ```

- **Rollback story:** `pip index versions contexel` / pin
  `contexel==<prior>`; a bad release is yanked on PyPI (yank, never delete)
  and followed by a patch release. No prior version exists for this first
  release; the first fallback is yanking.
- **Note:** local commits 0.1.14 (Pages enablement fix) and the 0.1.15
  release-day doc cleanup landed after the tag; they are docs/workflow-only
  and roll into the next tag.
