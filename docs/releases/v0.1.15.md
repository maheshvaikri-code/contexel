# Release record — contexel v0.1.15 (the context shaping plane)

- **Published:** 2026-07-26, via tag `v0.1.15` — the **first Trusted
  Publishing (OIDC) release with provenance attestations**; no API token
  involved. Both workflow jobs green: `test-build-publish` (full suite on
  the tagged commit, tag↔version guard, build, twine check, OIDC publish)
  and `post-publish-smoke` (clean-environment install of the published
  wheel + quickstart).
- **Live metadata verified** via `https://pypi.org/pypi/contexel/json`:
  version 0.1.15; wheel + sdist present; releases now 0.1.13 / 0.1.14 /
  0.1.15.
- **Ships:** async `@shaped`; `tokens.scoped()` tenant-isolated accounting;
  `tiktoken_tokenizer(model=)`; pipeline policy fingerprints;
  `trace(id_field=)` + `Trace.audit()` governance records; `allowlist` +
  `quarantine` injection boundary controls (the readiness audit's probe is
  a regression test). All G4-panel-reviewed.
- **Pre-flight evidence:** 64 tests passed on `PYTHONHASHSEED` 0 and 7;
  `twine check` PASSED on both artifacts (see the release commit message).
- **Follow-up:** the `PYPI_TOKEN` GitHub secret is now unused and can be
  deleted (Settings → Secrets and variables → Actions).
- **Rollback story:** yank on PyPI (never delete) + pin
  `contexel==0.1.14`; hotfix path per doctrine HOTFIX class.
