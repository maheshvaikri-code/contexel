# Release record — contexel v0.1.16 (TypeScript parity, dual-registry)

- **Published:** 2026-07-28, via tag `v0.1.16` (re-pointed to `82d6fbf`
  after a first tag push hit the version-must-match-tag guard exactly as
  designed — the guarded run published nothing, so re-pointing violated no
  never-re-release rule). **First release to ship BOTH packages from one
  tag:** PyPI (Trusted Publishing/OIDC + attestations) and npm — the
  first-ever `contexel` npm publish (token auth + `--provenance`
  attestations).
- **All 14 workflow jobs green:** `test` and `ts-test` (golden-vector
  regeneration + drift check + full TS suite) on Linux/Windows/macOS gated
  the publishes; `pypi-publish` → `npm-publish` (npm needs PyPI, so a
  failed Python release can never ship its mirror); `pypi-smoke` and
  `npm-smoke` installed the **published** artifacts on all three OSes and
  ran the quickstart.
- **Live metadata verified:** `pypi.org/pypi/contexel/0.1.16/json` →
  0.1.16; `npm view contexel` → 0.1.16,
  `registry.npmjs.org/contexel/-/contexel-0.1.16.tgz`.
- **Ships:** the `ts/` parity package (all 8 stages + merge, pipeline
  fingerprints, async-aware `shaped`, `trace`/`audit`, code-point token
  accounting, `AsyncLocalStorage`-scoped overrides) held to the Python
  canon by `parity/vectors.json` golden vectors incl. a 13-class edge
  block; Python canon: `_typed_tuple` container type tags,
  `trim_to_budget(min_records=)` empty-list guard, bare-string `fields`;
  the dual-registry 3-OS release pipeline itself.
- **Pre-flight evidence:** Python 67 passed, TypeScript 36/36 on the
  tagged tree; two-round adversarial G4 (REQUEST-CHANGES → fixes →
  APPROVE-WITH-NITS → NITs closed), incl. 118k-case `pyRound` fuzz and
  50k-double serializer fuzz against CPython, zero mismatches.
- **Rollback story:** PyPI — yank (never delete) + pin
  `contexel==0.1.15`; npm — `npm deprecate contexel@0.1.16 "<reason>"`
  (unpublish only within 72h; prefer deprecate + patch release); hotfix
  path per doctrine HOTFIX class.
